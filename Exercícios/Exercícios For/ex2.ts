//2
import PromptSync from "prompt-sync";
let prompt = PromptSync();
export function forex2() {
    for (let contador = 2; contador <= 10; contador += 2) {
        console.log(contador);
       
    }
}
